## セクション13: 運用設計

### 13.1 通知設計

`config/settings.yaml` の `notifications.enabled: true` を前提とする。

**チャネル:**

| 優先度 | チャネル | 理由 |
|--------|----------|------|
| 推奨 | Telegram Bot | モバイル通知・コマンド返信が可能、無料、Bot API が軽量 |
| 代替 | Slack Incoming Webhook | チーム運用時に適する |

**通知イベント一覧:**

| # | イベント | 重要度 | 送信タイミング | 内容例 |
|---|---------|--------|---------------|--------|
| 1 | 下書き生成完了 | 低 | Writer が `draft_queue.json` に追加時 | `[Draft] 新しい下書きが追加されました（スコア: 8.2）` |
| 2 | circuit_breaker 発動 | 高 | 連続エラーが `max_consecutive_errors`（デフォルト3）に到達時 | `[ALERT] Circuit Breaker発動: poster 3連続エラー` |
| 3 | kill_switch 発動 | 最高 | `scripts/kill_switch.py stop` 実行時 or Supervisor 自動停止時 | `[CRITICAL] Emergency Stop: 薬機法リスク検知` |
| 4 | コンプラリスクコメント検知 | 高 | Replier がコメント内に規制関連キーワードを検知時 | `[COMPLIANCE] 投稿 xxx にコンプラリスクコメント検知` |
| 5 | トークン期限7日前 | 高 | Supervisor の定期チェックで `token_state.json` の `expires_at` が7日以内 | `[TOKEN] Threads APIトークン期限まであと7日` |
| 6 | research_pool 24h未更新 | 中 | Supervisor チェック時に `research_pool.json` の `last_updated` が24h以上前 | `[WARN] research_pool が24時間以上更新されていません` |
| 7 | 日次KPIレポート | 低 | 毎日 `analyst.run_cron`（05:00）後に送信 | `[Daily] 投稿6件 / avg engagement 4.2% / フォロワー +12` |
| 8 | バズ検知 | 中 | Fetcher がバズ判定閾値（views 500+, engagement 5%+）超過を検知時 | `[BUZZ] 投稿 xxx がバズっています（views: 1,200）` |
| 9 | 多様性アラート | 中 | Analyst がカテゴリエントロピーの低下を検知時 | `[DIVERSITY] エントロピー 1.2 → 閾値1.5を下回りました` |

**通知送信の実装方針:**

- `core/notifier.py` に `Notifier` クラスを新設
- 各エージェントは `Notifier.send(event_type, message, severity)` を呼ぶ
- 重要度が「高」以上の場合、送信失敗時にリトライ（最大3回、指数バックオフ）
- 通知の重複抑制: 同一イベントは30分以内に再送しない

---

### 13.2 バックアップ戦略

| 対象 | 方法 | 保持期間 | 備考 |
|------|------|----------|------|
| `data/state/` | 日次zip圧縮 | 30世代（30日分） | `scripts/backup.py` で自動化 |
| `knowledge/` | git管理 | 無制限 | 変更時に手動 or CI で commit |
| `data/logs/` | ファイルローテーション | 7日保持 | `TimedRotatingFileHandler` で自動削除 |
| `config/` | git管理 | 無制限 | `.env` は `.gitignore`、`.env.example` のみ管理 |

**バックアップスケジュール:**

- `scripts/backup.py` を毎日 04:00 JST に実行（Task Scheduler or cron）
- バックアップ先: `data/backups/state_YYYYMMDD_HHMMSS.zip`
- 30世代を超えた古いバックアップは自動削除

**JSON破損時の復旧:**

```
1. 破損ファイルを .bak にリネーム保存
   例: post_queue.json → post_queue.json.bak.20260328_120000
2. StateManager._default_for() のデフォルトスキーマで新規作成
3. ログに [ERROR] として記録 + 通知送信（重要度: 高）
4. .bak ファイルから手動で必要なデータを救出可能
```

**`scripts/backup.py` の処理フロー:**

```python
# 概要（擬似コード）
def main():
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup_dir = Path("data/backups")
    backup_dir.mkdir(parents=True, exist_ok=True)

    # 1. data/state/ を zip 圧縮
    zip_path = backup_dir / f"state_{timestamp}.zip"
    shutil.make_archive(str(zip_path.with_suffix("")), "zip", "data/state")

    # 2. 古いバックアップを削除（30世代保持）
    backups = sorted(backup_dir.glob("state_*.zip"))
    for old in backups[:-30]:
        old.unlink()

    # 3. 完了ログ
    logger.info("Backup completed: %s", zip_path.name)
```

---

### 13.3 プロセス永続化（Windows環境）

#### NSSM（Non-Sucking Service Manager）設定手順

```powershell
# 1. NSSMのインストール
choco install nssm

# 2. サービス登録
nssm install ThreadsBot "C:\Python311\python.exe" "-m core.scheduler all"
nssm set ThreadsBot AppDirectory "C:\ThreadsBot"
nssm set ThreadsBot AppStdout "C:\ThreadsBot\data\logs\service_stdout.log"
nssm set ThreadsBot AppStderr "C:\ThreadsBot\data\logs\service_stderr.log"
nssm set ThreadsBot AppRotateFiles 1
nssm set ThreadsBot AppRotateBytes 10485760

# 3. サービス起動
nssm start ThreadsBot

# 4. ステータス確認
nssm status ThreadsBot
```

#### Task Scheduler 設定手順

```
1. タスクスケジューラを開く（taskschd.msc）
2. 「タスクの作成」→ 名前: ThreadsBot
3. トリガー: 「スタートアップ時」+ 「毎日 06:50」（二重保険）
4. 操作:
   - プログラム: C:\Python311\python.exe
   - 引数: -m core.scheduler all
   - 開始: C:\ThreadsBot
5. 設定:
   - 「タスクが既に実行中の場合は新しいインスタンスを開始しない」を選択
   - 「ユーザーがログオンしているかどうかにかかわらず実行する」を選択
```

#### Heartbeat 設計

- Supervisor が15分ごとに `system_state.json` の `last_health_check` を更新

```json
{
  "last_health_check": "2026-03-28T14:30:00+09:00",
  "agent_status": {
    "researcher": {"last_run": "2026-03-28T12:00:00+09:00", "status": "ok"},
    "writer":     {"last_run": "2026-03-28T13:15:00+09:00", "status": "ok"},
    "poster":     {"last_run": "2026-03-28T14:00:00+09:00", "status": "ok"},
    "fetcher":    {"last_run": "2026-03-28T08:00:00+09:00", "status": "ok"},
    "analyst":    {"last_run": "2026-03-28T05:00:00+09:00", "status": "ok"},
    "replier":    {"last_run": "2026-03-28T14:15:00+09:00", "status": "ok"},
    "supervisor": {"last_run": "2026-03-28T14:30:00+09:00", "status": "ok"}
  }
}
```

#### 外部監視

- 外部スクリプト or Uptime Kuma で `system_state.json` の `last_health_check` を監視
- **30分更新なし** → アラート通知 + プロセス再起動
- 再起動コマンド: `nssm restart ThreadsBot` or Task Scheduler の手動実行

---

### 13.4 ログ設計

**現状（V1）:** `core/logger.py` で日付ベースのファイルハンドラ + コンソール出力。

**V2 改善計画:**

| 項目 | V1 | V2 |
|------|----|----|
| ローテーション | 日付ファイル名（手動） | `TimedRotatingFileHandler`（`when="midnight"`, `backupCount=30`） |
| 形式 | テキスト行 | JSON Lines 形式（構造化ログ） |
| 保持 | 無制限 | 30日自動削除 |

**ログレベル定義:**

| レベル | 用途 | 例 |
|--------|------|-----|
| DEBUG | 開発・デバッグ用の詳細情報 | API レスポンス全文、スコア計算の中間値 |
| INFO | 正常な動作の記録 | 投稿成功、リサーチ完了、エージェント起動/終了 |
| WARNING | 想定内だが注意が必要な事象 | 品質スコア閾値ぎりぎり、API レートリミット接近 |
| ERROR | 処理失敗（リカバリ可能） | API 呼び出し失敗、JSON パースエラー、投稿失敗 |
| CRITICAL | システム停止レベルの障害 | Emergency Stop 発動、全エージェント停止 |

**JSON Lines 形式の例:**

```json
{"ts":"2026-03-28T14:30:00+09:00","level":"INFO","agent":"poster","msg":"Post published","post_id":"post_20260328_003","score":8.4}
{"ts":"2026-03-28T14:30:05+09:00","level":"ERROR","agent":"poster","msg":"API call failed","error":"429 Too Many Requests","retry":1}
```

**ログファイル構成:**

```
data/logs/
├── threadsbot_2026-03-28.log      # メインログ（全エージェント）
├── threadsbot_2026-03-27.log
├── ...（最大30ファイル）
├── service_stdout.log              # NSSMサービス標準出力
└── service_stderr.log              # NSSMサービス標準エラー
```

---

### 13.5 ダッシュボードCLI

`scripts/dashboard.py` — ターミナルで現在のBot状態を一覧表示するCLIツール。

**実行方法:**

```bash
python scripts/dashboard.py
python scripts/dashboard.py --watch    # 30秒ごとに自動更新
```

**モックアップ表示:**

```
=== ThreadsBot Dashboard ===
Status    : RUNNING (heartbeat: 2分前)
Account   : ゆう（スキンケア成分メディア）
Today     : 3/6 posts | Next: 14:15 JST
Queues    : draft 4件 / post 2件 / research 18件

--- Agents ---
  researcher : 12:00 OK     (4h間隔)
  writer     : 13:15 OK     (キュー補充済)
  poster     : 14:00 OK     (次回 14:15)
  fetcher    : 08:00 OK     (6h間隔)
  analyst    : 05:00 OK     (日次)
  replier    : 14:15 OK     (30min間隔)
  supervisor : 14:30 OK     (15min間隔)

--- Performance (7d) ---
  Avg Engagement : 4.2%
  Top Category   : skincare_ingredients (38%)
  Entropy        : 1.82 / 2.32 (目標)
  Followers      : 342 (+18 this week)

--- Alerts ---
  None

--- Safety ---
  Emergency Stop : inactive
  Circuit Breaker: inactive
  Daily Posts    : 3 / 6
  Token Expires  : 52 days
```

**データソース:**

| 表示項目 | 読み取り元 |
|----------|-----------|
| Status / heartbeat | `data/state/system_state.json` → `last_health_check` |
| Today / posts | `data/state/system_state.json` → `daily_counters` |
| Next post | `data/state/post_queue.json` → `queue[0].scheduled_at` |
| Queues | `draft_queue.json`, `post_queue.json`, `research_pool.json` の件数 |
| Agents | `system_state.json` → `agent_status` |
| Performance | `data/state/performance.json` |
| Alerts | 各種チェック結果の集約 |
| Safety | `system_state.json`, `token_state.json` |

---

### 13.6 障害復旧手順書（RUNBOOK概要）

| # | 障害 | 検知方法 | 復旧手順 |
|---|------|----------|----------|
| 1 | Threads API 認証切れ | Poster/Fetcher の 401 エラー、トークン期限通知 | 1. Meta Developer Console でトークン再発行 2. `.env` の `THREADS_ACCESS_TOKEN` を更新 3. `data/state/token_state.json` を更新 4. `python -m core.scheduler poster` で動作確認 |
| 2 | Claude API quota 超過 | Writer/Analyst の 429 エラー | 1. Anthropic Console で使用量確認 2. 待機（日次リセットまで）or プラン変更 3. `config/settings.yaml` の `max_generation_attempts` を一時的に下げて消費抑制 |
| 3 | YouTube API quota 超過 | Researcher の 403 エラー | 1. Google Cloud Console で quota 確認 2. 翌日の自動リセットを待つ（太平洋時間 0:00） 3. 緊急時は別プロジェクトの API キーに切替 |
| 4 | JSON 破損 | StateManager のパースエラー、ログの `[ERROR] Failed to load` | 1. 破損ファイルを `.bak` にリネーム 2. `scripts/init_data.py` でデフォルト値から再作成 3. `data/backups/` から直近の zip を解凍して必要データを復旧 |
| 5 | プロセスクラッシュ | heartbeat 30分未更新、外部監視アラート | 1. `data/logs/` で直近のエラーログを確認 2. `nssm restart ThreadsBot` or Task Scheduler で手動実行 3. 原因がOOM等なら `system_state.json` の `daily_counters` を確認して再開 |
| 6 | 炎上・苦情 | コンプラリスクコメント通知、手動発見 | 1. `python scripts/kill_switch.py stop "炎上対応"` で即停止 2. 該当投稿を Threads アプリから手動削除 3. 必要に応じて謝罪投稿を手動作成 4. 原因分析後、NGワード追加等の再発防止策を実施 5. `python scripts/kill_switch.py clear` で再開 |

---

### 13.7 kill_switch 運用ルール

`scripts/kill_switch.py` による緊急停止の運用基準。

**トリガーとアクション:**

| # | トリガー | アクション | 停止方法 |
|---|---------|-----------|----------|
| 1 | 薬機法違反疑い | 即停止 + 該当投稿を手動削除 | `python scripts/kill_switch.py stop "薬機法リスク"` |
| 2 | 苦情コメント | 状況確認 → 必要に応じ停止 | 確認後 `python scripts/kill_switch.py stop "苦情対応"` |
| 3 | 異常課金（Claude API等） | 即停止 + API キー無効化 | `python scripts/kill_switch.py stop "異常課金検知"` |
| 4 | 1時間N件エラー | Supervisor が自動停止 | 自動: `auto_kill_threshold: 10`（1時間に10件以上のエラーで自動発動） |
| 5 | 手動判断（その他） | 任意のタイミングで停止可能 | `python scripts/kill_switch.py stop "理由"` |

**モバイル停止（Telegram コマンド）:**

- Telegram Bot に `/stop 理由` を送信 → `kill_switch.py stop` と同等の処理を実行
- `/status` で現在の状態を確認
- `/clear` で停止解除

**停止中の挙動:**

- `system_state.json` の `emergency_stop: true` が設定される
- 全エージェントが `SafetyGuard.can_post()` で停止状態を確認し、処理をスキップ
- Supervisor は停止中も heartbeat 更新を継続（死活監視のため）
- 停止解除は人間の明示的な操作が必須（自動解除しない）

**自動停止の設計:**

```yaml
# config/settings.yaml に追加予定
supervisor:
  auto_kill_threshold: 10        # 1時間あたりのエラー件数
  auto_kill_window_minutes: 60   # 集計ウィンドウ
```

- Supervisor が `system_state.json` のエラーカウントを監視
- ウィンドウ内のエラーが閾値を超えた場合、`SafetyGuard.emergency_stop()` を自動実行
- 自動停止時は通知を送信（重要度: 最高）

---

## セクション14: KPI・マイルストーン

### 14.1 KPIダッシュボード

| # | KPI | 計測方法 | 目標（6ヶ月後） |
|---|-----|---------|----------------|
| 1 | フォロワー数 | Threads API `followers_count` | 3,000 |
| 2 | 日次平均いいね | `performance.json` から7日移動平均 | 50/投稿 |
| 3 | エンゲージメント率 | `(likes + replies) / views * 100` | 5% |
| 4 | プロフクリック率 | `profile_clicks / impressions * 100`（Threads Insights） | 3% |
| 5 | 月次アフィリ収益 | ASP管理画面から手動記録 | 30,000円 |
| 6 | 品質スコア平均 | `performance.json` の `quality_score` 平均 | 8.0以上 |
| 7 | approve率 | `draft_queue.json` の承認/却下比率 | 90%以上 |
| 8 | カテゴリエントロピー | Analyst のエントロピー計算 | 1.8以上（5カテゴリ均等で log2(5)=2.32） |

**計測頻度:**

- リアルタイム: フォロワー数、エンゲージメント（Fetcher 経由）
- 日次: KPI #1-6 を `performance.json` に記録、Analyst が日次レポート生成
- 週次: KPI #7-8 を含む週次サマリーを Telegram 通知
- 月次: 収益の手動記録 + 全KPIの月次振り返り

---

### 14.2 マイルストーン

| 期間 | フォロワー | 累計投稿数 | エンゲージメント率 | 月次収益 | 主要アクション |
|------|-----------|-----------|-------------------|---------|---------------|
| 1ヶ月 | 300 | 120 | 3% | 0円 | コンテンツ品質の安定化、ブーストモード運用、hook_stock 蓄積 |
| 3ヶ月 | 1,000 | 450 | 4% | 5,000円 | アフィリエイト導線開始、LP公開、TOP5/BOTTOM3フィードバック確立 |
| 6ヶ月 | 3,000 | 900 | 5% | 30,000円 | テーマツリー動的更新、テンプレート最適化、X/Instagram巡回開始 |
| 12ヶ月 | 10,000 | 1,800 | 5%+ | 100,000円 | ジャンル横展開検討、note有料記事、コミュニティ形成 |

**マイルストーン達成判定:**

- 各マイルストーンの達成/未達は月次レビューで人間が判断
- 未達の場合は原因分析 → 施策調整（自動判断しない）

---

### 14.3 撤退基準

| 条件 | 判定時期 | アクション |
|------|---------|-----------|
| フォロワー 200未満 | 3ヶ月時点 | ジャンル変更を検討（成分知識 → 別切り口） |
| エンゲージメント率 1%未満 | 3ヶ月時点 | コンテンツ方針の抜本的見直し |
| 月次収益 10,000円未満 | 6ヶ月時点 | アフィリエイト導線の再設計 or 撤退判断 |

**重要原則:**

- **撤退判断は必ず人間が行う**（自動撤退しない）
- 数値が基準を下回った場合、まず原因分析を行い、改善施策を1ヶ月試してから最終判断
- 撤退 = アカウント削除ではなく、Bot運用の停止（コンテンツ資産は保持）

---

## セクション15: 実装優先順位

### Phase 0: ナレッジ基盤（Week 1）

Botの判断基盤となるナレッジファイルを整備する。コードの変更なし。

| # | タスク | 変更ファイル | 依存関係 |
|---|--------|-------------|----------|
| 0-1 | プロフィール定義 | `knowledge/profile.yaml`（新規） | なし |
| 0-2 | ターゲット定義 | `knowledge/target.yaml`（新規） | なし |
| 0-3 | ジャンル定義 | `knowledge/genre.yaml`（新規） | なし |
| 0-4 | ドメイン知識 | `knowledge/domain_knowledge.yaml`（新規） | なし |
| 0-5 | テーマツリー | `knowledge/theme_tree.yaml`（新規） | 0-3 |
| 0-6 | 投稿ルール整備 | `knowledge/posting_rules.md`（更新） | 0-1, 0-2 |
| 0-7 | NGワード拡充 | `config/ng_words.txt`（更新） | なし |
| 0-8 | ブランド名リスト | `knowledge/brand_names.txt`（新規） | なし |

---

### Phase 1: コア機能改修（Week 2-3）

品質評価の刷新とフロー変更。最も重要なフェーズ。

| # | タスク | 変更ファイル | 依存関係 |
|---|--------|-------------|----------|
| 1-1 | 品質採点7項目化 | `prompts/evaluator.txt`, `agents/writer.py`, `core/quality_gate.py` | 0-6 |
| 1-2 | NGワード正規表現対応 | `core/safety.py`, `config/ng_words.txt` | 0-7 |
| 1-3 | TF-IDF類似度修正 | `core/quality_gate.py` | なし |
| 1-4 | 薬機法AI判定 | `core/safety.py`, `prompts/compliance_check.txt`（新規） | 0-4, 0-8 |
| 1-5 | draft_queue実装 | `core/state_manager.py`, `agents/writer.py` | 1-1 |
| 1-6 | review CLI実装 | `scripts/review.py`（新規） | 1-5 |
| 1-7 | Poster連携（draft→post） | `agents/poster.py` | 1-5 |
| 1-8 | 投稿上限6件/日に変更 | `config/settings.yaml` | なし |
| 1-9 | ペルソナ除去（V1→V2） | `prompts/writer.txt`, `config/tone.yaml`, `agents/writer.py` | 0-1 |
| 1-10 | performance.json バグ修正 | `agents/fetcher.py`, `agents/analyst.py` | なし |

---

### Phase 2: フィードバック & 計測（Week 4-5）

投稿後のデータ収集と改善ループの確立。

| # | タスク | 変更ファイル | 依存関係 |
|---|--------|-------------|----------|
| 2-1 | Fetcher 3段階計測（1h/6h/24h） | `agents/fetcher.py`, `config/settings.yaml` | なし |
| 2-2 | メトリクス時系列記録 | `agents/fetcher.py`, `core/state_manager.py` | 2-1 |
| 2-3 | 初速アクション（バズ→アフィリ） | `agents/fetcher.py`, `agents/replier.py` | 2-1 |
| 2-4 | TOP5+BOTTOM3分析 | `agents/analyst.py` | 2-2 |
| 2-5 | プロンプト注入（実績→Writer） | `agents/writer.py`, `prompts/writer.txt` | 2-4 |
| 2-6 | レビュー傾向分析 | `agents/analyst.py` | 1-6 |
| 2-7 | エントロピー計算 | `agents/analyst.py` | 2-4 |
| 2-8 | hook_stock 収集 | `scripts/add_hook.py`（新規）, `knowledge/hook_stock.json`（新規） | なし |
| 2-9 | Writer連携（hook_stock利用） | `agents/writer.py` | 2-8 |

---

### Phase 3: 運用基盤（Week 5-6）

安定運用のための通知・監視・バックアップ基盤。

| # | タスク | 変更ファイル | 依存関係 |
|---|--------|-------------|----------|
| 3-1 | Telegram通知実装 | `core/notifier.py`（新規）, `config/settings.yaml` | なし |
| 3-2 | Telegramレビュー（approve/reject） | `core/notifier.py`, `scripts/review.py` | 1-6, 3-1 |
| 3-3 | バックアップスクリプト | `scripts/backup.py`（新規） | なし |
| 3-4 | ダッシュボードCLI | `scripts/dashboard.py`（新規） | なし |
| 3-5 | NSSMサービス化 | ドキュメントのみ（手動設定） | なし |
| 3-6 | RUNBOOK作成 | `docs/RUNBOOK.md`（新規） | なし |
| 3-7 | ログ改善（JSON Lines + ローテーション） | `core/logger.py` | なし |
| 3-8 | CTA PR表記対応 | `agents/writer.py`, `prompts/writer.txt` | 0-6 |
| 3-9 | コンプラ検知（コメント監視） | `agents/replier.py` | 3-1 |

---

### Phase 4: 拡張（Week 7+）

成長加速のための追加機能。Phase 1-3が安定してから着手。

| # | タスク | 変更ファイル | 依存関係 |
|---|--------|-------------|----------|
| 4-1 | X巡回エージェント | `agents/x_crawler.py`（新規）, `services/x_api.py`（新規） | 2-8 |
| 4-2 | Instagram巡回 | `agents/ig_crawler.py`（新規）, `services/ig_api.py`（新規） | 2-8 |
| 4-3 | アウトバウンドリプライ | `agents/replier.py` | Phase 3完了 |
| 4-4 | テーマツリー動的更新 | `agents/analyst.py`, `knowledge/theme_tree.yaml` | 2-4, 2-7 |
| 4-5 | テンプレート最適化 | `agents/writer.py`, `config/ugc_templates.yaml` | 2-5 |
| 4-6 | セットアップウィザード | `scripts/setup_wizard.py`（新規） | Phase 1完了 |
| 4-7 | LP（成分条件チェッカー） | 別リポジトリ | Phase 2完了 |

---

## セクション16: V1→V2 移行計画

### 16.1 移行方針

**一括切替方式（Big Bang）を採用する。並行運用は行わない。**

理由:
- V1はまだ本番投稿を開始していない（開発段階）
- マルチアカウント構造を残したまま改修すると、全ての変更が複雑化する
- 切り戻しは git で容易に可能

移行ブランチ: `v2-migration`

---

### 16.2 削除対象

| # | 対象 | パス | 削除理由 |
|---|------|------|----------|
| 1 | りくアカウント設定 | `config/accounts/riku/` | マルチアカウント廃止 |
| 2 | ひなアカウント設定 | `config/accounts/hina/` | マルチアカウント廃止 |
| 3 | UGCテンプレート | `config/ugc_templates.yaml` | V2では使用しない |
| 4 | クロスポスト設定 | `config/cross_posting.yaml` | マルチアカウント廃止 |
| 5 | クロスポスターエージェント | `agents/cross_poster.py` | マルチアカウント廃止 |
| 6 | クロスポスタープロンプト | `prompts/cross_poster.txt` | マルチアカウント廃止 |
| 7 | アカウントコンテキスト | `core/account_context.py` | マルチアカウント廃止 |

---

### 16.3 既存テストの仕分け

| # | テストファイル | カテゴリ | 対応 |
|---|---------------|---------|------|
| 1 | `test_state_manager.py` | 状態管理 | V2更新: `draft_queue.json` スキーマ追加、`cross_post_state.json` 削除 |
| 2 | `test_safety.py` | 安全装置 | V2更新: NGワード正規表現、薬機法AI判定のテスト追加 |
| 3 | `test_quality_gate.py` | 品質評価 | V2更新: 7項目採点、TF-IDF修正のテスト追加 |
| 4 | `test_poster.py` | 投稿 | V2更新: draft_queue→post_queue フロー対応 |
| 5 | `test_fetcher.py` | メトリクス取得 | V2更新: 3段階計測対応 |
| 6 | `test_replier.py` | 返信 | V2更新: コンプラ検知機能追加 |
| 7 | `test_supervisor.py` | 監視 | V2更新: auto_kill_threshold、heartbeat テスト追加 |
| 8 | `test_boost.py` | ブーストモード | V2更新: 軽微な設定値変更のみ |
| 9 | `test_token_manager.py` | トークン管理 | V2更新: 変更なし（そのまま利用可能） |
| 10 | `test_cross_poster.py` | クロスポスト | 全削除: マルチアカウント廃止に伴い不要 |
| 11 | `test_account_context.py` | アカウント切替 | 全削除: マルチアカウント廃止に伴い不要 |

---

### 16.4 マイグレーション手順

```
1. git branch v2-migration && git checkout v2-migration
   - V2移行用ブランチを作成

2. ナレッジファイル作成（Phase 0）
   - knowledge/profile.yaml, target.yaml, genre.yaml,
     domain_knowledge.yaml, theme_tree.yaml, brand_names.txt を新規作成
   - knowledge/posting_rules.md を V2方針に更新

3. tone.yaml 分離
   - config/tone.yaml をV2の1アカウント構成に書き換え
   - ペルソナ（ゆう/りく/ひな）の口調定義を削除し、
     匿名物知り系の口調定義に統一

4. 削除対象の削除（セクション16.2の全ファイル）
   - config/accounts/riku/, config/accounts/hina/
   - config/ugc_templates.yaml, config/cross_posting.yaml
   - agents/cross_poster.py, prompts/cross_poster.txt
   - core/account_context.py

5. コード改修（Phase 1のタスク）
   - 品質採点7項目化、NGワード正規表現、TF-IDF修正
   - 薬機法AI判定、draft_queue実装、Poster連携
   - 投稿上限変更、ペルソナ除去、performance.jsonバグ修正

6. テスト更新・実行
   - test_cross_poster.py, test_account_context.py を削除
   - 残りのテストをV2仕様に更新
   - pytest tests/ -v で全テストが通ることを確認

7. migrate_v1_to_v2.py でデータ拡張
   - data/state/ の既存JSONに新フィールドを追加
   - cross_post_state.json を削除
   - performance.json のスキーマを V2 形式に変換

8. 動作確認
   - python -m core.scheduler researcher で単体テスト
   - python -m core.scheduler writer で下書き生成確認
   - python scripts/dashboard.py で状態表示確認
   - 各エージェントを順次実行し、ログにエラーがないことを確認

9. 本番切替
   - git merge v2-migration を main にマージ
   - .env のトークン・APIキーを本番値に設定
   - python -m core.scheduler all で全エージェント起動
   - 初回投稿を手動で確認してから自動運用開始
```

---

### 16.5 V1マルチアカウント廃止の意思決定ログ

**廃止理由:**

| # | 理由 | 詳細 |
|---|------|------|
| 1 | 0→1000段階では集中すべき | 3アカウント分散より1アカウントに全リソースを集中した方が成長が速い |
| 2 | 運用負荷 | 3つのペルソナの口調管理、投稿スケジュール調整、クロスポスト制御は初期段階で過剰 |
| 3 | 架空ペルソナの信頼リスク | 「ゆう」「りく」「ひな」は架空の人格であり、発覚時の信頼毀損リスクがある |
| 4 | ステマ規制上のリスク | 同一運営者による複数アカウントの相互言及は、ステマ規制に抵触する可能性がある |

**将来方針:**

- アーキテクチャはジャンル非依存に設計する（スキンケア固有ロジックをハードコードしない）
- 将来の横展開は「1ジャンル1アカウント」原則で対応
  - 例: スキンケア成分Bot、ヘアケア成分Bot、食品添加物Bot
  - 各Botは独立したインスタンスとして運用（同一コードベース、別設定ファイル）
- マルチアカウント「機能」の再実装は、1アカウントで月10万円を達成してから検討
- その際は `account_context.py` のような切替機構ではなく、Docker等でのインスタンス分離を推奨
